<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- First Drawable (with padding) -->
    <item android:drawable="@drawable/icon_square" />

    <!-- Second Drawable (with padding) -->
    <item android:drawable="@drawable/icon_circle"
        android:start="6dp"
        android:end="6dp"
        android:top="6dp"
        android:bottom="6dp"/>

</layer-list>